[project]
name = "dotacni-majak-red-history"
version = "0.1.0"
description = "Historical ReD grant connector for Dotační maják"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
