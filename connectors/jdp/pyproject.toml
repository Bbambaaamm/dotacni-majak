[project]
name = "dotacni-majak-jdp"
version = "0.1.0"
description = "Jednotný dotační portál MF public dashboard source adapter for Dotační maják"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
