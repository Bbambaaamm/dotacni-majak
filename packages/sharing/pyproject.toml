[project]
name = "dotacni-majak-sharing"
version = "0.1.0"
description = "Revocable read-only project sharing domain for Dotační maják"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
