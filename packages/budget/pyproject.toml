[project]
name = "dotacni-majak-budget"
version = "0.1.0"
description = "Provider-agnostic usage budget guard for Dotační maják"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
