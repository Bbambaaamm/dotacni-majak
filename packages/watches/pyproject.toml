[project]
name = "dotacni-majak-watches"
version = "0.1.0"
description = "Project Watch matching for Dotační maják"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
