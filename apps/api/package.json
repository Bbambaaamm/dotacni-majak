{
  "name": "@dotacni-majak/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "dev": "wrangler dev --local --port 8787 --persist-to ../../.wrangler/local"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^5.0.1",
    "wrangler": "^4.0.0"
  }
}
